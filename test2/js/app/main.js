define(["jquery", "header", "footer"], function($) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    $(function() {
      $.fn.homePage();
      $.fn.alpha();
      $.fn.beta();
    });
});
